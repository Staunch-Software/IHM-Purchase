SECRET_KEY=change-me-to-a-long-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Either fill in DB_USER/DB_PASSWORD/DB_HOST/DB_PORT/DB_NAME below, or set
# DATABASE_URL directly (e.g. postgresql+asyncpg://user:pass@host:5432/dbname)
# to override them. DATABASE_URL is NOT shell-expanded, so don't reference
# the other vars inside it.
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ihm_purchase

SMARTPAL_EMAIL=
SMARTPAL_PASSWORD=
SMARTPAL_BASE_URL=https://smartpal.ozellar.com/Home/Account/Index

SCRAPER_CRON_HOUR=2
SCRAPER_CRON_MINUTE=0

CORS_ORIGINS=http://localhost:5173
