services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ihm_purchase
    ports:
      - "5432:5432"
    volumes:
      - ihm_purchase_pgdata:/var/lib/postgresql/data

volumes:
  ihm_purchase_pgdata:
